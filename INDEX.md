# Python 科研代码库 — 完整索引

> **环境要求**: Python 解释器使用 ArcGIS Pro 3.6.3 克隆环境 (`D:\GD\arcgispro_clone\python.exe`)

---

## 目录导航

| 分类 | 路径 | 主要领域 |
|------|------|---------|
| 01 | [机器学习建模](./01_机器学习建模/) | XGBoost、SHAP、随机森林、特征选择 |
| 02 | [空间计量模型](./02_空间计量模型/) | SAR/SEM/SDM、MGWR 地理加权回归 |
| 03 | [空间自相关分析](./03_空间自相关分析/) | 全局/局部 Moran's I、LISA |
| 04 | [地理探测器](./04_地理探测器/) | 交互探测热图、数据准备 |
| 05 | [栅格处理](./05_栅格处理/) | 裁剪、NoData处理、空值填充 |
| 06 | [景观生态分析](./06_景观生态分析/) | PLADJ/SPLIT、土地利用、植被分类 |
| 07 | [耦合协调度分析](./07_耦合协调度分析/) | 破碎化-干旱耦合度、空间关系 |
| 08 | [植被与生态](./08_植被与生态/) | NDVI趋势、转移矩阵、偏相关 |
| 09 | [水文土壤](./09_水文土壤/) | 水源涵养、土壤导水率、土壤模拟 |
| 10 | [统计分析与可视化](./10_统计分析与可视化/) | PCA、相关性、直方图、洛伦茨曲线 |
| 11 | [数据整理](./11_数据整理/) | POI转点、GD因子整理、表格导出 |

---

## 01. 机器学习建模

### `xgboost_shap_基础.py`（合并自 XGBoost+SHAP.py + 归一化_XGboost+shap.py）
**功能**: 训练 XGBoost 回归模型 + SHAP 可解释性分析

**输入**:
- Excel 文件（含因变量和自变量列），通过 `file_path` 和 `sheet_name` 配置
- 可选参数 `normalize=True` 对特征做 MinMax 归一化

**输出**:
- 控制台输出模型 MSE
- SHAP summary plot、force plot、dependence plot（需取消注释）
- shap_values 数组

**使用步骤**:
1. 修改 `file_path` 为你的 Excel 路径
2. 修改 `sheet_name` 为对应年份
3. 确保第一列是因变量，其余是自变量
4. 运行脚本

---

### `xgboost_shap_多年份批量.py`（原 SHAP.py）
**功能**: 加载预训练的多年份 XGBoost 模型（pkl），批量生成 SHAP 解释图 + 部分依赖图

**输入**:
- 预训练模型文件 `xgb_model_{年份}.pkl`，存放在 `model_save_folder`
- Excel 多 Sheet 数据，`sheet_names` 如 `["2001","2005","2010","2015","2020"]`

**输出**:
- SHAP summary plot 图片
- 部分依赖图 (PDP) 3×4 布局，保存为 `pdp_plot_{year}.png`

**前置依赖**: 需先运行模型训练脚本生成 pkl 文件

---

### `xgboost_shap_批量CSV.py`（原 空间建模/xgboost+shap.py）
**功能**: 批量处理文件夹下所有 CSV，逐文件训练 XGBoost 并生成 SHAP 图

**输入**:
- CSV 文件夹（第一列为因变量 y，第 2-17 列为自变量 X）
- 自动检测并跳过含中文 `<空>` 的缺失值

**输出**:
- 每个 CSV 对应的 `{文件名}_shap.png`（SHAP beeswarm 图）
- 每个 CSV 对应的 `{文件名}_importance.png`（特征重要性排序图）

**特性**: 样本量 > 200,000 时自动随机降采样

---

### `xgboost_shap_高级可视化.py`（原 空间建模/测试.py）
**功能**: 高阶学术级 SHAP 可视化——复合蜂群图 + 全特征 GAM 平滑依赖图

**输入**:
- 单个 CSV 文件，通过 `csv_path` 和 `TARGET_COL` 配置
- 自动将 UID 列设为索引，自动 log1p 转换偏态目标变量

**输出**:
- `{TARGET_COL}_Enhanced_Summary.png` — 含背景全局重要性条形的复合蜂群图
- `{TARGET_COL}_GAM_Dependence_All.png` — 全部特征的 GAM 平滑 SHAP 依赖图（自动标记零交叉阈值和中位数）

**依赖**: `pygam` 库

---

### `多模型对比_最优选择.py`（原 浙江省县级人均居民收入建模.py）
**功能**: 7 种回归模型全面对比，自动选出最优模型

**支持的模型**: 随机森林、GBDT、SVR、多元线性回归、KNN、XGBoost、LightGBM

**输入**:
- Excel 文件，通过 `excel_path` 配置
- 第一列为索引，第二列为因变量，后续列为自变量

**输出**:
- 控制台打印各模型 R²/RMSE/MAE 排名
- 最优模型的特征重要性/系数/排列重要性
- 最优模型保存为 `best_spatial_model.pkl`

**特性**: 10 折交叉验证 + 超参数网格搜索 + 自动排除"曲率"变量

---

### `随机森林_交叉验证.py`（原 随机森林.py）
**功能**: 简单版随机森林，10 折交叉验证评估

**输入**: Excel 文件（第二列为因变量，第三列起为自变量）

**输出**: 控制台输出每折 R² 和平均 R²

---

### `共线性诊断_特征选择.py`（原 共线性测试建模.py）
**功能**: VIF 共线性诊断 → LassoCV 特征选择 → 随机森林建模 → 特征重要性

**输入**:
- Excel 文件，`optimal_features` 变量列出所有候选特征
- 第一列为因变量

**输出**:
- `vif_analysis.png` — VIF 条形图
- `lasso_mse_vs_alpha.png` — Lasso 的 α-MSE 曲线
- `rf_feature_importance.png` — 随机森林特征重要性
- `lasso_feature_selection_results.csv` — 特征筛选结果
- `rf_feature_importance.csv` — 特征重要性排序

---

## 02. 空间计量模型

### `空间自回归_全流程.py`（原 空间自回归.py）
**功能**: OLS → LM 诊断 → SAR → SEM → SDM 全流程自动执行

**输入**:
- Shapefile 点/面数据，通过 `file_path` 配置
- `y_col` 为因变量字段名，`x_cols` 为自变量列表

**输出**:
- LM 检验结果（LM Error / LM Lag / Robust LM Error / Robust LM Lag）
- 自动判断推荐模型类型
- SAR / SEM / SDM 三个模型的估计结果
- `Model_Comparison_12w.csv` — 模型对比汇总表

**依赖**: `libpysal`, `spreg`

---

### `空间杜宾模型_效应分解.py`（合并自 空间杜宾模型.py + 空间杜宾模型效应分解.py）
**功能**: SDM 模型估计 + LeSage & Pace 直接/间接/总效应分解

**输入**:
- Shapefile 数据，`y_col` 为因变量，`x_cols` 为自变量列表
- 自动构建 KNN 空间权重（k=8）

**输出**:
- 控制台输出完整的效应分解表（直接系数/滞后系数/直接效应/间接效应/总效应 + 显著性星号）
- `SDM_Final_Report_12w.csv` — 最终效应分解表格

**特性**: 自动过滤高共线性 WX 变量（相关系数>0.98），自动计算间接效应占比

**依赖**: `libpysal`, `spreg`, `scipy`

---

### `地理加权回归_MGWR.py`（合并自 地理加权回归.py + 地理加权回归_抽样.py）
**功能**: MGWR 多带宽地理加权回归，自动区分全局/局部变量

**输入**:
- 多年份 Shapefile，通过 `my_tasks` 列表配置（路径 + 因变量 + 年份标签）
- `max_sample_size` 参数控制最大样本量（默认 10000），超出自动随机抽样

**输出**:
- 一个包含多 Sheet 的 Excel 文件，每个年份包含：
  - `{年份}_地理变异` — 各变量带宽和全局/局部判定
  - `{年份}_全局变量` — 全局变量系数和 T 值
  - `{年份}_局部变量` — 局部变量描述统计（均值/标准差/分位数）+ 模型 R²/AICc

**依赖**: `mgwr` 库

---

## 03. 空间自相关分析

### `全局莫兰指数_批量.py`（原 全局莫兰指数.py）
**功能**: 批量计算点/面数据的全局 Moran's I

**输入**:
- 存放 Shapefile 的文件夹
- `target_field` 为计算字段，`k_neighbors` 为 KNN 邻居数（默认 8）

**输出**:
- `Moran_Results.csv` — 每个文件的 Moran's I / Z分数 / P值 / 显著性判断

---

### `局部空间自相关_LISA.py`（原 景观格局指数空间自相关分析.py）
**功能**: 栅格级别全局 + 局部 Moran's I，输出 LISA 分类栅格

**输入**:
- 存放 TIFF 栅格的文件夹
- 自动剔除 0/NoData/NaN 像元

**输出**:
- 每个栅格对应的 `{文件名}_LISA_Clust.tif`（1=HH, 2=LH, 3=LL, 4=HL，0=不显著）
- `Global_Moran_Stats.xlsx` — 全局统计汇总表

---

### `LISA结果转栅格.py`（保留原名）
**功能**: 将 LISA 分析的点 Shapefile 结果转为栅格 TIFF

**输入**:
- 存放 LISA 结果 Shapefile 的文件夹（需含 COType 字段，值为 LL/LH/HH/HL）
- 模板栅格（用于获取坐标系和范围）

**输出**:
- 每个 Shapefile 对应的分类栅格（LL=1, LH=2, HH=3, HL=4，背景=0）

---

### `空间自相关_ArcPy版.py`（原 空间自相关分析.py）
**功能**: 使用 ArcPy 工具箱执行栅格→点→局部异常值分析→全局自相关

**输入**:
- 存放 TIFF 栅格的文件夹

**输出**:
- LISA 分类栅格
- `Global_Morans_I_Results.csv` — 全局 Moran 统计表

**依赖**: ArcPy Spatial Analyst 扩展

---

## 04. 地理探测器

### `交互探测_热图绘制.py`（原 交互探测绘图.py）
**功能**: 读取地理探测器输出 CSV，Python 重绘学术风格交互热图

**输入**:
- `result_dir` 文件夹，自动搜索 `交互探测结果_*.csv` 和 `因子探测结果_*.csv`

**输出**:
- `Python版_交互热图_{名称}.png` — 高 DPI (600) 热图，含 q 值标注和显著性星号
- 星号含义：`*` = 双因子增强，`**` = 非线性增强
- 字体：Times New Roman（英文）+ 宋体（中文图注）

---

### `渔网分区统计_数据准备.py`（原 地理探测器/渔网分区统计.py）
**功能**: 为地理探测器准备输入数据——计算渔网内 PM2.5 均值和各地类面积占比

**输入**:
- 渔网矢量（含唯一标识字段）
- PM2.5 栅格、CLCD 土地利用栅格

**输出**:
- CSV 表格，包含 Zone_ID / y（PM2.5均值）/ x1-x8（耕地-湿地占比）

**依赖**: ArcPy Spatial Analyst 扩展

---

## 05. 栅格处理

### `批量裁剪_按矢量范围.py`（原 多个栅格根据多个范围批量裁剪.py）
**功能**: 使用矢量中每个要素的范围批量裁剪所有栅格

**输入**:
- 栅格文件夹 + 矢量文件（含多个多边形）+ 命名字段

**输出**:
- 裁剪后的栅格，命名格式 `{矢量ID值}_{栅格名}.tif`

**依赖**: ArcPy Spatial Analyst

---

### `批量裁剪_按字段.py`（原 按照矢量字段裁剪栅格.py）
**功能**: 按属性表字段值逐个选中矢量要素并裁剪栅格

**输入**:
- 单个栅格 + 含多个要素的矢量 + 命名字段

**输出**:
- 裁剪后的栅格，命名格式 `{栅格名}_{字段值}.tif`

**依赖**: ArcPy

---

### `异常值处理_负999转NoData.py`（原 异常值-999写入nodata.py）
**功能**: 批量将栅格中的 -999 异常值替换为真正的 NoData 标记

**输入**:
- 存放 TIFF 的文件夹

**输出**:
- 处理后的 TIFF（同名输出到新文件夹），LZW 压缩

**特性**: 多进程并行 + 分块读写（block_windows），适合大栅格

---

### `NoData填充0.py`（原 nodata.py）
**功能**: 将栅格中 NoData 像元全部填充为 0

**输入**: 栅格文件夹

**输出**: 处理后的栅格（IsNull → Con → 0）

**依赖**: ArcPy Spatial Analyst

---

### `空值迭代填充.py`（原 自动填充空置.py）
**功能**: 迭代填充栅格内部空值（NoData），直到填满或达最大迭代次数

**输入**:
- 地理数据库 (GDB) 中的栅格 + 矢量掩膜

**输出**:
- 填充完成后的 TIFF 栅格

**算法**: 每次用 10×10 窗口的焦点均值填充当前 NoData 区域，迭代至无空值

**依赖**: ArcPy Spatial Analyst

---

## 06. 景观生态分析

### `景观指数_PLADJ_SPLIT.py`（原 计算PLADJ和SPLIT.py）
**功能**: 分块计算大栅格的 PLADJ（相似邻接比）和 SPLIT（分离指数）

**输入**:
- 土地利用分类栅格 + 目标地类编码（如耕地=1, 林地=2）
- `output_res_meters` 输出分辨率（默认 1000m）

**输出**:
- `{地类}_SPLIT.tif` 和 `{地类}_PLADJ.tif`

**算法**: 分窗口扫描 → 8 邻域连通区标记 → 计算景观指数

---

### `景观指数_渔网尺度调试.py`（原 按照矢量渔网计算景观格局指数.py）
**功能**: 调试用脚本，逐网格检查景观指数计算过程

**输入**:
- 土地利用 TIFF + 渔网 Shapefile + 目标像素值
- `DEBUG_INDEX` 指定要调试的网格 ID

**输出**:
- 详细的中间过程输出（坐标转换、窗口读取、二值化、掩膜、pylandstats 计算结果）

---

### `土地利用重分类.py`（原 土地利用地类转换.py）
**功能**: CLCD 9 类重分类为 7 类

**重分类规则**:

| 原值 | 含义 | 新值 |
|------|------|------|
| 1 | 耕地 | 1 |
| 2 | 林地 | 2 |
| 3 | 灌木 | 3 |
| 4 | 草地 | 4 |
| 5 | 水体 | 5 |
| 6 | 冰雪 | 5（合并到水体） |
| 7 | 裸地 | 6 |
| 8 | 不透水面 | 7 |
| 9 | 湿地 | 5（合并到水体） |

**依赖**: ArcPy Spatial Analyst

---

### `土地利用强度.py`（原 计算土地利用强度.py）
**功能**: 根据 CLCD 编码首位数字计算土地利用强度等级

**规则**: 1→3, 2/3/4→2, 5→4, 其他→0

**依赖**: ArcPy Spatial Analyst

---

### `植被分类_林地非林地.py`（原 划分林地非林地.py）
**功能**: CLCD 重分类为林地(1)/非林地(2)

**规则**: CLCD 编码 2/3/4（林地/灌木/草地）→ 1，其他 → 2

**依赖**: ArcPy Spatial Analyst

---

## 07. 耦合协调度分析

### `耦合度协调度_栅格尺度.py`（原 计算耦合度和协调度.py）
**功能**: 基于耕地破碎化和 TVDI 干旱化栅格，计算耦合度 C 和协调度 D

**输入**:
- `masked_fragmentation_{year}.tif` 和 `TVDI_Cropland_{year}.tif`

**输出**:
- `Coupling_Degree_C_Value_{year}.tif` — C 值栅格
- `Coordination_Degree_D_Value_{year}.tif` — D 值栅格
- `Coupling_Degree_C_Class_{year}.tif` — C 分级 (1-4)
- `Coordination_Degree_D_Class_{year}.tif` — D 分级 (1-4)

**分级阈值**:
- C: [0, 0.3)→1, [0.3, 0.5)→2, [0.5, 0.8)→3, [0.8, 1.0]→4
- D: [0, 0.3)→1, [0.3, 0.6)→2, [0.6, 0.8)→3, [0.8, 1.0]→4

---

### `耦合度协调度_渔网尺度.py`（原 耦合度协调度渔网尺度计算.py）
**功能**: 渔网尺度的耦合度与协调度计算

**输入**:
- 破碎化和 TVDI 栅格（自动重投影到 UTM 48N）

**输出**:
- `Coupling_Coordination_{year}.shp` — 渔网矢量，含 U_Val / E_Val / C_Value / D_Value / C_Class / D_Class

**特性**: 300m 渔网，自动清理临时文件

---

### `空间关系类型判断.py`（合并自 按照渔网判断耦合变化类型.py + 耕地破碎化和干旱话空间类型判断.py）
**功能**: 基于破碎化和干旱化等级，判断空间耦合关系和滞后类型

**支持两种模式**:
- **栅格模式**: 直接读取分类栅格，输出空间关系栅格和滞后类型栅格
- **矢量模式**: 读取耦合协调度渔网结果，输出含 Type_A 和 Type_B 字段的矢量

**输出编码**:
- Type_A（空间关系）: 11-55（十位=破碎化等级，个位=干旱等级）
- Type_B（滞后类型）: 1=同步, 2=生态滞后, 3=破碎化滞后

---

## 08. 植被与生态

### `植被区域合成_5年期.py`（原 重新划分植被无植被区域i.py）
**功能**: 将逐年植被数据合成 5 年期二值栅格

**规则**: 5 年内 ≥ 3 年有植被 → 判为植被（1），否则非植被（0）

**输出**:
- `Vegetation_Composite_{start}_{end}.tif`（如 2000_2004）

---

### `NDVI分区统计_人工区自然区.py`（原 统计人工区自然区NDVI.py）
**功能**: 按人工区/自然区矢量范围，逐年统计 NDVI 的最大/最小/均值/标准差

**输入**:
- NDVI 栅格文件夹（命名格式 `Landsat_{year}_NDVI.tif`）
- 人工区矢量文件

**输出**:
- `NDVI_Stats_Unified.csv` — 包含每年人工区和自然区的 Max/Min/Mean/Std

---

### `植被面积转移矩阵.py`（合并自 面积转移矩阵.py + 面积转移矩阵_合成五期.py）
**功能**: 计算多时段植被面积转移矩阵

**输入**:
- 植被分类栅格（0=无, 1=有），支持两期或多期合成数据

**输出**:
- 转移栅格（编码: 0=无→无, 1=无→有, 2=有→无, 3=有→有）
- `Vegetation_Transition.csv` — 各转移类型面积（km²）

**特性**: 自动重投影到 Albers 等面积投影确保面积精度

---

### `NDVI趋势分析_Sen_MK.py`（原 泰森斜率估计.py）
**功能**: 基于多年 NDVI 时间序列，计算 Sen's Slope 和 Mann-Kendall 趋势检验

**输入**:
- NDVI 栅格时间序列（`Landsat_{year}_NDVI.tif`，2000-2025）

**输出**:
- `MK_Trend_Class.tif` — 趋势分级（-4~4，负值下降，正值上升，绝对值越大越显著）
- `Sen_Slope_Value.tif` — 斜率值

**特性**: 自动 WarpedVRT 对齐不同年份栅格

---

### `偏相关分析_空间栅格.py`（原 栅格数据偏相关分析.py）
**功能**: 计算空间栅格的偏相关系数（控制其他因子后，FVC 与单个因子的净相关）

**输入**:
- 多年份目标变量栅格（如 `FVC_{year}.tif`）和因子栅格（如 `pre_{year}.tif`）
- 静态因子（如 DEM/坡度）不带年份后缀

**输出**:
- `Spatial_Pcor.csv` — 包含 Year / Factor / Partial_Correlation / P_Value / Significance 五列

**特性**: 使用 pingouin 计算偏相关，手动 t 检验获取 P 值，>200,000 样本自动抽样

---

## 09. 水文土壤

### `水源涵养量计算.py`（保留原名）
**功能**: InVEST 模型水源涵养量计算

**公式**: `Retention = V_factor × T1_factor × K_factor × wyield`

**输入**:
- 静态栅格: `T1.tif` (地形指数), `K.tif` (土壤饱和导水率)
- 动态栅格: `v_{year}.tif` (流速系数), `wyield_{year}.tif` (产水量)

**输出**:
- `Retention_{year}.tif` — 水源涵养量栅格

**依赖**: ArcPy Spatial Analyst

---

### `土壤导水率_K因子.py`（原 计算K因子.py）
**功能**: Saxton (2006) 方法计算土壤饱和导水率 Ks

**输入**:
- Excel 文件，需含 SAND（砂粒%）、CLAY（粘粒%）、OM（有机质%）三列

**输出**:
- `soil_data_with_K_cmd.xlsx` — 原数据 + K_cm_d 列（饱和导水率，cm/d）

**计算过程**: 萎蔫系数(θ1500) → 田间持水量(θ33) → 饱和含水量(θS) → 导水率(Ks)

---

### `土壤发生模拟器_云南.py`（原 空间建模/土壤发生模拟器.py）
**功能**: 基于云南省立体气候的土壤类型预测模拟器

**输入**: 海拔(m)、年降水(mm)、发育阶段（初期/中期/成熟期）

**输出**: 预测土壤类型 + 成土特征描述 + 剖面结构

**分类逻辑**:
- <800m + 高降水 → 砖红壤
- 800-2500m → 红壤/黄壤
- 2500-3500m → 棕壤/暗棕壤
- ≥3500m → 高山草甸土/寒漠土

---

## 10. 统计分析与可视化

### `主成分分析_PCA.py`（合并自 主成分分析.py + 主成分分析绘图.py）
**功能**: 对矢量属性表执行 PCA 降维 + Biplot 可视化

**输入**:
- Shapefile 路径 + 分析字段列表 + 主成分数量

**输出**:
- 控制台输出各主成分解释方差比
- PCA Biplot 散点图（含特征向量箭头和解释方差表格）
- 可选保存含 PC 得分的结果 Shapefile

---

### `相关性分析_热力图.py`（原 相关性分析.py）
**功能**: Pearson 相关系数矩阵 + 学术热力图（含显著性星号）

**输入**:
- Excel 文件 / Shapefile 属性表 + 分析指标列表

**输出**:
- 相关性热力图 PNG（只显示下三角，含 *p<0.05, **p<0.01 标注）
- 相关系数矩阵 Excel

---

### `相关性分析_多年份多层表头.py`（原 空间建模/相关性.py）
**功能**: 自动识别因子基础名称，按年份分别计算 Pearson 相关性

**输入**:
- Shapefile 属性表，含多年份变量（如 `tmp_2000`, `tmp_2010`, `tmp_2020`）

**输出**:
- `Correlation_Results.xlsx` — 多层表头格式（年份×[Pearson, 显著性]），含显著性星号

---

### `相关性分析_三指数产沙量.py`（原 空间建模/相关性分析.py）
**功能**: 三个生态指数（CSI/CCI/SPI）与水蚀/风蚀产沙量的相关性

**输入**:
- 含多个流域 Sheet 的 Excel 文件

**输出**:
- `流域三指数与产沙量相关性分析结果.xlsx` — 各流域/年份/因子的格式化相关系数

---

### `逐步回归_全变量OLS.py`（原 空间建模/逐步回归分析.py）
**功能**: 按年份执行全变量 OLS 回归 + VIF 诊断

**输入**:
- Shapefile 属性表 + 按年份显式定义的自变量列表

**输出**:
- 多 Sheet Excel，每年份一个 Sheet，含系数 / T值 / P值 / VIF + 模型 R²/F 值

---

### `栅格直方图_绘制.py`（合并自 直方图.py + 直方图组图.py）
**功能**: 支持单栅格或多栅格组图的直方图绘制

**使用模式**:
- 单图模式: 调用 `plot_tif_histogram(tif_path)`
- 组图模式: 调用 `plot_multiple_tif_histograms(tif_paths)` 绘制 N×1 排列的多图

---

### `洛伦茨曲线.py`（保留原名）
**功能**: 绘制文化遗产/设施空间分布的洛伦茨曲线（累积面积 vs 累积密度）

**输入**:
- 城市矢量（含 name + Shape_Area 字段）
- 点位数据（含类型字段，如文化遗产分类）

**输出**:
- `Lorenz_Curve_{类型}.png` — 各类型的洛伦茨曲线图

---

### `POI批量转点.py`（原 批量poi转点.py）
**功能**: 将 CSV 格式的 POI 数据批量转为 ArcGIS 点要素类

**输入**:
- POI 的 CSV 文件夹（需含经度、纬度、大类字段）

**输出**:
- 每个 POI 大类一个要素类，存入指定 GDB

**依赖**: ArcPy

---

## 11. 数据整理

### `GD因子表格整理.py`（原 空间建模/整理GD因子所需表格.py）
**功能**: 从宽表 Excel 中按年份提取 y 和 x 因子列，输出地理探测器所需 CSV

**输入**:
- Excel 文件，列名格式如 `esv_{year}`, `pop_{year}`, `tmp_{year}`, `DEM`, `SLOPE` 等

**输出**:
- `data_{year}.csv` — 每年份一个 CSV，列名重命名为 y / x1-x8

---

### `MGWR结果表格导出.py`（原 临时.py）
**功能**: 将 MGWR 运行结果（硬编码数据）导出为多 Sheet 格式化的 Excel 表格

**输出**:
- 5 个 Sheet：系数统计 / 模型诊断 / 变量与邻域 / 带宽汇总 / 带宽搜索历史

---

## 文件对照表

| 整理后文件名 | 原始文件 |
|-------------|---------|
| `01_机器学习建模/xgboost_shap_基础.py` | `XGBoost+SHAP.py` + `归一化_XGboost+shap.py` |
| `01_机器学习建模/xgboost_shap_多年份批量.py` | `SHAP.py` |
| `01_机器学习建模/xgboost_shap_批量CSV.py` | `空间建模/xgboost+shap.py` |
| `01_机器学习建模/xgboost_shap_高级可视化.py` | `空间建模/测试.py` |
| `01_机器学习建模/多模型对比_最优选择.py` | `浙江省县级人均居民收入建模.py` |
| `01_机器学习建模/随机森林_交叉验证.py` | `随机森林.py` |
| `01_机器学习建模/共线性诊断_特征选择.py` | `共线性测试建模.py` |
| `02_空间计量模型/空间自回归_全流程.py` | `空间自回归.py` |
| `02_空间计量模型/空间杜宾模型_效应分解.py` | `空间杜宾模型.py` + `空间杜宾模型效应分解.py` |
| `02_空间计量模型/地理加权回归_MGWR.py` | `空间建模/地理加权回归.py` + `空间建模/地理加权回归_抽样.py` |
| `03_空间自相关分析/全局莫兰指数_批量.py` | `全局莫兰指数.py` |
| `03_空间自相关分析/局部空间自相关_LISA.py` | `景观格局指数空间自相关分析.py` |
| `03_空间自相关分析/LISA结果转栅格.py` | `LISA结果转栅格.py` |
| `03_空间自相关分析/空间自相关_ArcPy版.py` | `空间自相关分析.py` |
| `04_地理探测器/交互探测_热图绘制.py` | `交互探测绘图.py` |
| `04_地理探测器/渔网分区统计_数据准备.py` | `地理探测器/渔网分区统计.py` |
| `05_栅格处理/批量裁剪_按矢量范围.py` | `多个栅格根据多个范围批量裁剪.py` |
| `05_栅格处理/批量裁剪_按字段.py` | `按照矢量字段裁剪栅格.py` |
| `05_栅格处理/异常值处理_负999转NoData.py` | `异常值-999写入nodata.py` |
| `05_栅格处理/NoData填充0.py` | `nodata.py` |
| `05_栅格处理/空值迭代填充.py` | `自动填充空置.py` |
| `06_景观生态分析/景观指数_PLADJ_SPLIT.py` | `计算PLADJ和SPLIT.py` |
| `06_景观生态分析/景观指数_渔网尺度调试.py` | `按照矢量渔网计算景观格局指数.py` |
| `06_景观生态分析/土地利用重分类.py` | `土地利用地类转换.py` |
| `06_景观生态分析/土地利用强度.py` | `计算土地利用强度.py` |
| `06_景观生态分析/植被分类_林地非林地.py` | `划分林地非林地.py` |
| `07_耦合协调度分析/耦合度协调度_栅格尺度.py` | `计算耦合度和协调度.py` |
| `07_耦合协调度分析/耦合度协调度_渔网尺度.py` | `耦合度协调度渔网尺度计算.py` |
| `07_耦合协调度分析/空间关系类型判断.py` | `按照渔网判断耦合变化类型.py` + `耕地破碎化和干旱话空间类型判断.py` |
| `08_植被与生态/植被区域合成_5年期.py` | `重新划分植被无植被区域i.py` |
| `08_植被与生态/NDVI分区统计_人工区自然区.py` | `统计人工区自然区NDVI.py` |
| `08_植被与生态/植被面积转移矩阵.py` | `面积转移矩阵.py` + `面积转移矩阵_合成五期.py` |
| `08_植被与生态/NDVI趋势分析_Sen_MK.py` | `泰森斜率估计.py` |
| `08_植被与生态/偏相关分析_空间栅格.py` | `栅格数据偏相关分析.py` |
| `09_水文土壤/水源涵养量计算.py` | `水源涵养量计算.py` |
| `09_水文土壤/土壤导水率_K因子.py` | `计算K因子.py` |
| `09_水文土壤/土壤发生模拟器_云南.py` | `空间建模/土壤发生模拟器.py` |
| `10_统计分析与可视化/主成分分析_PCA.py` | `主成分分析.py` + `主成分分析绘图.py` |
| `10_统计分析与可视化/相关性分析_热力图.py` | `相关性分析.py` + `特征清洗1222.py` |
| `10_统计分析与可视化/相关性分析_多年份多层表头.py` | `空间建模/相关性.py` |
| `10_统计分析与可视化/相关性分析_三指数产沙量.py` | `空间建模/相关性分析.py` |
| `10_统计分析与可视化/逐步回归_全变量OLS.py` | `空间建模/逐步回归分析.py` |
| `10_统计分析与可视化/栅格直方图_绘制.py` | `直方图.py` + `直方图组图.py` |
| `10_统计分析与可视化/洛伦茨曲线.py` | `空间建模/洛伦茨曲线.py` |
| `10_统计分析与可视化/POI批量转点.py` | `批量poi转点.py` |
| `11_数据整理/GD因子表格整理.py` | `空间建模/整理GD因子所需表格.py` |
| `11_数据整理/MGWR结果表格导出.py` | `临时.py` |

> **注**: 以下原始文件未纳入整理：`main.py`（PyCharm 模板）、`测试.py`（仅导入 BFAST）、`特征重要性.py`（空文件）、`重新空间自回归模型.py`（ArcGIS 源码副本）、`ndvi_res.txt`（空文件）、`GD.txt`（数据文件）、`sample.xlsx`（数据文件）、`空间建模/土壤发生模拟器.html`（HTML 文件）、`空间建模/测试1.py`（与测试.py 类同）、`空间建模/__pycache__`（缓存目录）。
